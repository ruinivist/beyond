# beyond

## Development

Enable the pre-commit checks once per clone:

```bash
git config core.hooksPath .githooks
```

Commits then run the Dart formatting check and `flutter analyze` automatically.
