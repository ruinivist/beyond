name: plane
description: plane
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  infinite_lazy_grid:
    git: https://github.com/ruinivist/infinite_lazy_grid.git
  re_editor: ^0.10.0
  re_highlight: ^0.0.3
  scribble: ^0.10.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
