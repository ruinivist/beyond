name: plane
description: plane
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_markdown_plus: ^1.0.12
  flutter_markdown_plus_latex: ^1.0.5
  google_fonts: ^6.3.3
  infinite_lazy_grid:
    git: https://github.com/ruinivist/infinite_lazy_grid.git
  re_editor: ^0.10.0
  re_highlight: ^0.0.3
  scroll_animator: 0.3.0
  scribble: ^0.10.0+1
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_math_fork: ^0.7.4
  flutter_lints: ^6.0.0
  url_launcher_platform_interface: ^2.3.2

flutter:
  uses-material-design: true
