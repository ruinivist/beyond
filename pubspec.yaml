name: plane
description: plane
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  infinite_lazy_grid:
    git: https://github.com/ruinivist/infinite_lazy_grid.git

dev_dependencies:
  flutter_lints: ^6.0.0
