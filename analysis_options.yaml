include: package:very_good_analysis/analysis_options.10.3.0.yaml

analyzer:
  exclude:
    - '**/*.g.dart'

linter:
  rules:
    # This is an application, not a package published for external use.
    public_member_api_docs: false
